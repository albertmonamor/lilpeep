#include <iostream>
#include <string.h>
#include "peep.h"
#include "explorer.h"
#include <fstream>
#pragma warning(disable:4996)

using namespace std;



/* ---written by: אברהם סבן
*  ---date: 23.09.2023
*  ---please dont copy
*  ---talk to me: +972585005617
*  ---for unix OS
*
*/

using namespace std;

int main()
{
    /* ===== INIT ===== */
    getPeepName(PEEP_NAME);
    // peep tr;
    // tr.run();
    listDrives("dfsdf");
    
    // map<string, pair<size_t, char*>> package;
	// for (size_t i = 0; i < (SUM_BLOCKS * SUM_BLOCKS); i++) {
	// 	package[to_string(i)] = make_pair(0, nullptr);
	// }

    // while (1){
    //     //auto wSize = GetWindowSize();
    //     //free(get_img_24_bit_jpg(wSize.first, wSize.second,50).second);
    //     auto d = set_X_parts_image(package, 50,SUM_BLOCKS,1743, 945).second;
    //     //deallocate_receive_proto(d);
    //     this_thread::sleep_for(chrono::milliseconds(200));
    // }

}

